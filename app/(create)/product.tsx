import { styles } from '@/lib/ui'
import { Surface, Text } from 'react-native-paper'

const CreateProduct = () => {
  return (
    <Surface style={styles.screen}>
      <Text>Hello CreateProduct</Text>
    </Surface>
  )
}

export default CreateProduct
